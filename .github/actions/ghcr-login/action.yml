name: "Log in to GHCR"
description: "Logs in to the GitHub Container Registry using the workflow's GITHUB_TOKEN"
runs:
  using: "composite"
  steps:
    - uses: docker/login-action@v4
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ github.token }}
